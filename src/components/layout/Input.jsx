import React, { Component } from 'react';
const Input = (props) => {
    return (
        <React.Fragment>
            <input type={props.type} />
        </React.Fragment>
    );
}

export default Inputprop;